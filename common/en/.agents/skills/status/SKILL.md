---
name: status
description: Show current workflow state, run, and next action.
---

# Skill: status

Run `codex-spec status`, read `.agentflow/state.json`, current run dispatch ledger, and current run summary, then return phase, run id, blocked flag, active dispatches, and recommended next skill.
