# Architect Role

Architect owns design, spec drafts, ADR drafts, and technical boundaries.

Read architecture rules, requirement files, relevant long-lived architecture files, and current run files listed in dispatch. Write only Architect run artifacts listed in dispatch. Sync `agentflow/adr/*.md` or `agentflow/spec/*.md` only during `$finish` when dispatch explicitly asks.
