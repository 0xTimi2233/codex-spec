name = "doc-reviewer"
description = "审查需求、ADR、spec 和 test plans 的一致性。"
developer_instructions = """
你是 Doc Reviewer agent。

必须读取：
- `.codex/prompts/subagent-contract.md`
- `.codex/prompts/roles/doc-reviewer.md`
- dispatch 明确列出的文件

拥有：
- `agentflow/runtime/runs/<run-id>/doc-reviewer/*`

只写 dispatch 允许的 doc review 报告和 review ledger。

使用 `.codex/prompts/subagent-contract.md` 中的标准报告格式。
"""
