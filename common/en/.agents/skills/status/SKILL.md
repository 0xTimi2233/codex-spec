---
name: status
description: Show current workflow state, run, and next action.
---

# Skill: status

Run `codex-spec status`, read `.agentflow/state.json` and current run summary, then return phase, run id, blocked flag, and recommended next skill.
