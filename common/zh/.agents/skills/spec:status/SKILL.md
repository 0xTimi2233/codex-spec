---
name: spec:status
description: 查看当前 workflow state、planning session、run 和下一步建议。
---

# Skill: spec:status

执行 `codex-spec status`，返回 phase、planning track、planning session、run id、blocked flag、未结束调度和建议的下一个 skill。
